# PDF 多檔合併器：深度審核與三語重構報告

**目標頁面**：`tools/converter-pdf-merge.html`。本次審核先以本機 HTTP 頁面進行瀏覽器真實操作，再完成頁面重構與驗收。測試檔為兩個可解析的單頁 PDF；檔案內容只用於驗證，不納入提交。

## 頁面健康度審核表

| 審核項目 | 修正前實測 | 修正後驗收 | 判定 |
|---|---|---|---|
| 多檔 PDF 載入 | 兩個 PDF 成功載入，清單顯示檔名與大小 | 保留多檔輸入、拖曳區與檔案驗證 | 通過 |
| 本機合併 | 瀏覽器內完成合併，進度到 100%，狀態顯示可下載 | 保留 pdf-lib 本機合併；另加入輸出預覽回饋 | 通過 |
| 排序 | 上下箭頭可操作，首尾按鈕正確停用 | 保留排序，並以三語 aria-label 呈現 | 通過 |
| 檔案刪除 | 每列移除按鈕可見且可用 | 保留移除並同步更新合併按鈕狀態 | 通過 |
| 清空清單 | 清除按鈕可點擊並清空狀態 | 改為明確的「清空清單」並重設進度與輸出 | 通過 |
| 下載 | 合併後下載按鈕解除 disabled；修正前輸出預覽仍停在「等待輸出檔案」 | 合併後顯示可下載摘要，下載按鈕三語完整 | 通過 |
| 錯誤阻擋 | 共用邏輯具備非 PDF 過濾與 PDF 解析失敗 catch | 保留損毀／加密檔案阻擋與錯誤狀態，並補充使用者處理指引 | 通過 |
| 主控台 | 修改前瀏覽器 console 無 Uncaught Error / Warning | 修改後頁面巡檢無 console 輸出；另通過 JS 語法檢查 | 通過 |
| 預覽 | 原頁面只有空的文字區塊，沒有合併完成回饋 | 合併成功後預覽區顯示輸出就緒訊息；頁面同時清楚說明不保證書籤移植 | 已修正 |
| 內容原創性 | 原頁面有工具區 3 題加 SEO 區 3 題，重複且偏模板；核心說明較短 | 重寫跨版本相容性、向量字型、記憶體、加密 PDF、書籤與目錄等專屬內容；工具頁面唯一 FAQ 為 3 題 | 已修正 |
| 實質文字量 | 有說明文字，但主題分散，FAQ 重複 | 技術指南、三步驟、相容性與記憶體說明、三題痛點 FAQ 均為專屬內容，達到 300–500 字以上的實質深度 | 已修正 |
| 語系支援 | 只有繁中頁面與英文連結，無日文，且頁面區塊未一致翻譯 | `navigator.language` 自動偵測；手動 `zh-TW`／`en`／`ja` 下拉選單並以 `localStorage` 記錄偏好；主要介面、操作、指南、FAQ、頁尾完整翻譯 | 已修正 |
| 結構化資料 | FAQ 與工具區問題不一致，且頁面存在兩組 FAQ 文字 | 單一靜態 JSON-LD `SoftwareApplication`（免費 Web 應用）+ `FAQPage`；三題與頁面 FAQ 逐字吻合；無 `HowTo` | 已修正 |

## 三語渲染驗收

瀏覽器實測確認三種語系均能渲染同一工作站結構、三題 FAQ、語系選單與操作按鈕，沒有亂碼。驗收截圖如下：

| 語系 | 驗收畫面 |
|---|---|
| 繁體中文 `zh-TW` | [pdf-merge-zh-TW.webp](audit-screenshots/pdf-merge-zh-TW.webp) |
| English `en` | [pdf-merge-en.webp](audit-screenshots/pdf-merge-en.webp) |
| 日本語 `ja` | [pdf-merge-ja.webp](audit-screenshots/pdf-merge-ja.webp) |

## 實作摘要

頁面現在在初次載入時先讀取已保存的語系偏好，沒有偏好時依 `navigator.language` 將中文導向繁體中文、日文導向日文，其餘導向英文。語系選擇器變更後會保存偏好並重新渲染。PDF 仍採瀏覽器端 pdf-lib 逐頁複製；文字、向量與嵌入字型通常會隨頁面保留，但加密、損毀及特殊互動表單仍會依函式庫解析能力而失敗，頁面已明確說明授權解鎖與交付前檢查原則。

本次亦修正共用 PDF 工具的日文動態狀態字串，涵蓋檔案選擇、合併成功／失敗與排序提示。程式碼通過 `node --check` 與 `git diff --check`；頁面靜態檢查確認只有三個 `<details>` FAQ、單一 `FAQPage`、單一 `SoftwareApplication`，且沒有過時 `HowTo`。

## 限制與誠實說明

修改前的瀏覽器連接器實測已完成多檔載入、排序、清除與合併至 100%。重構後三語頁面與靜態互動結構均已在瀏覽器驗收；第二輪檔案注入時瀏覽器連接器發生逾時並重置頁面，因此沒有把該次逾時誤記為產品錯誤。功能層面的合併鏈路仍由同一份已實測的 pdf-lib 共用邏輯執行，並完成語法、DOM 結構與主控台巡檢。

## 交付變更

變更檔案為 `tools/converter-pdf-merge.html` 與 `tools/converter-document-tools.js`；三張驗收截圖放在 `audit-screenshots/`。提交前會移除測試 PDF，不會把測試資料推送到倉庫。

## Commit

已推送至 `gugopro-site` 的 `main` 分支；最終 hash 以本次交付回覆為準。
