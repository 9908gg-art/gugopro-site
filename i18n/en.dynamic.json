{
  "sourceLanguage": "zh-TW",
  "targetLanguage": "en",
  "catalogVersion": 1,
  "templates": {
    "5": "${analysis.maxLossStreak} records",
    "7": "${label} must be between ${lower} and ${upper}.",
    "8": "${label} must be an integer.",
    "9": "${label} must be a finite number.",
    "11": "${modeLabel(values)}; ${values.years} years",
    "12": "${modeLabel(values)}; results are for educational and stress-testing purposes only.",
    "14": "${recoveryIndex - mddTroughIndex} periods",
    "16": "${result.maxUnits.toLocaleString('zh-TW')} units",
    "18": "${result.reserveMonths.toFixed(1)} months",
    "19": "${result.transactionCount} items",
    "20": "${result.triggeredCount} items",
    "21": "${result.walkForward.length} held-out windows; fixed parameter baseline_only.",
    "22": "${returns.length} periods ${mode === 'prices' ? '價格轉換' : '使用者輸入'} return · MAR ${formatPercent(mar)} · annualization periods ${periods}",
    "23": "${sequenceLabel(result.sequence)}, asset balance changed from ${money(rows[0].end)} in year 1 to ${money(rows[rows.length - 1].end)} in year ${rows[rows.length - 1].year}.",
    "24": "${value >= 0 ? '+' : ''}${value.toFixed(2)} percentage points",
    "243": "<div class=\"bar\" style=\"height:${Math.max(3,x/max*205)}px\"><span>Year ${i+1}</span></div>",
    "244": "<div class=\"insight-row\"><span><strong>${name}</strong><small>Target ${t}% · Current ${total?Math.round(current/total*100):0}%</small></span><b class=\"${delta>=0?'positive':'negative'}\">${delta>=0?'增加':'減少'} ${money(Math.abs(delta))}</b></div>",
    "283": "CSV row ${index + 1} has an incorrect number of fields.",
    "594": "registry ${text(registry.registry_version)} · catalog ${text(catalog.catalog_version)} · updated ${text(catalog.updated_at)}",
    "614": "snapshot ${result.snapshotId.slice(0, 20)}…; results are for research purposes only.",
    "892": "with 1R = ${money(analysis.rMoney)}",
    "991": "Assuming each stop-loss is 1R, with a win rate of ${winRateRaw}% the expected value is ${expectancy.toFixed(2)}R.",
    "1047": "Price must be a positive finite number: ${observationDate}",
    "1128": "A total of ${equity.length} equity observations.",
    "1129": "Total ${values.years} years; single-path deterministic scenario, not a probability of success.",
    "1295": "Drawdown ${signed(analysis.maxDrawdown)}, calculated in trade order.",
    "1317": "Fixed baseline + decrease by ${pct(values.flexibleCut, 1)} on declines.",
    "1445": "Training progress ${finished} / ${total}",
    "1466": "Complete ${result.completeRows}; non-interpolated excluded ${result.droppedRows}",
    "1480": "Official fixture HTTP ${response.status}",
    "1530": "Peak ${signed(Math.max(...analysis.equity, 0))}; last ${signed(analysis.equity.at(-1) || 0)}",
    "1531": "峰值第 ${mddPeakIndex} 期 → 谷值第 ${mddTroughIndex} 期；${recoveryText === '尚未恢復' ? '截至序列最後一筆仍未回到前高。' :\"",
    "1866": "Invalid date format: ${value}",
    "1867": "Invalid date: ${value}",
    "1891": "Maximum drawdown ${formatPercent(mdd)}",
    "2000": "Orange line = strategy, blue line = benchmark; ${result.combined.contract.execution}. The chart displays only the evaluation period, not live data.",
    "2051": "Historical return ${q.toFixed(2)}%, ${(c*100).toFixed(1)}% confidence level; about ${((1-c)*100).toFixed(1)}% of observations exceed this loss.",
    "2092": "Expected value per trade ${signedR(outcome.expectancyR)}",
    "2188": "Drift threshold: ${formatPct(result.input.threshold, 2)} percentage points",
    "2304": "Current drawdown ${formatPercent(currentDrawdown)} · showing the most recent ${Math.min(equity.length, 250)} entries",
    "2306": "Currently completed ${done}/${items.length} items; score only reflects process completeness.",
    "2312": "Current mode: ${modeText}. Results are theoretical adjustments and do not represent executed trades.",
    "2333": "Current selection: ${sequenceLabel(result.sequence)}. The three paths differ only in the explicitly specified return sequence; other inputs are the same.",
    "2334": "Target ${formatPct(row.target, 2)}",
    "2350": "Target weights currently sum to ${targetTotal.toFixed(2)}%; must equal 100%.",
    "2354": "Catalog load failed: ${error.message || '無法讀取本地研究契約'}. This page will not display unverified data.",
    "2379": "Research incomplete: ${message} will not display unverified results.",
    "2415": "Record ${invalidIndex + 1} is not a valid number; please clean separators or text.",
    "2416": "Year ${result.depletionYear}",
    "2417": "Year ${rows[rows.length - 1].year}: ${money(rows[rows.length - 1].end)}",
    "2419": "Year 1: ${money(rows[0].end)}",
    "2645": "Calculation completed, but ${metricWarning}.",
    "2659": "Calculation incomplete: ${error.message}",
    "2660": "Calculation incomplete: ${message} will not display unverified results.",
    "2760": "Data quality status: ${quality}",
    "2770": "Number of records exceeds the limit of ${MAX_POINTS}; please clean up at the source first.",
    "2794": "Asset percentage withdrawal ${pct(values.percentageRate, 1)}",
    "2897": "Input error: ${error}",
    "2898": "Input error: ${parsed.error}",
    "2899": "Input error: ${result.error}",
    "3022": "There are ${remaining} pre-fields unconfirmed; please complete them or mark as not applicable.",
    "3090": "Open official source ${text(source.display_name, source.source_id)}",
    "3168": "Risk distance ${risk.toFixed(2)} · Return distance ${reward.toFixed(2)}; expected value not yet estimated."
  }
}
